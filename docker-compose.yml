services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: superlabs
      POSTGRES_PASSWORD: superpassword
      POSTGRES_DB: superlabs_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
